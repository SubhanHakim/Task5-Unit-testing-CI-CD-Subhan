FROM node:18-alpine

WORKDIR /app

# Copy all configuration files first
COPY package*.json ./
COPY tsconfig.json ./
COPY jest.config.cjs ./

# Install dependencies
RUN npm install

# Copy all source files
COPY . .

# Verify files exist
RUN ls -la && \
    ls -la tests/

# Run tests
CMD ["npm", "test"]